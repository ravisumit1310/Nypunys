const errorMessageTitle = "Sorry something happened !";

enum LocalStorageKeys {
  sessionId,
  clientId,
  userData,
  isDarkTheme,
  isFirstTime,
  enableFingerPrint,
  selectedWatchlistIndex,
  notification
}
